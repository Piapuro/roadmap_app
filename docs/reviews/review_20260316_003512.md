# コードレビューレポート — #020 ダッシュボード差分（TodoList・お知らせ置換）

**レビュー日時**: 2026-03-16
**対象**（マイルストーン→今日やること・フェーズ進捗→お知らせ 置換後の差分）:
- `src/types/dashboard.ts`（TodoItem / Announcement 追加）
- `src/client/components/dashboard/TodoListCard.tsx`（新規、MilestoneCard 置換）
- `src/client/components/dashboard/AnnouncementCard.tsx`（新規、PhaseProgressCard 置換）

**前回レビュー**: `docs/reviews/review_20260315_234316.md`
**レビュアー**: Claude Code (code-review skill)

---

## 📊 サマリー

| 重要度 | 件数 |
|--------|------|
| 🔴 Critical | 0 |
| 🟠 Major    | 1 |
| 🟡 Minor    | 2 |
| 🟢 Good     | 5 |
| 🔵 Suggestion | 1 |

**総合評価**: Excellent（置換は正しく完遂。前回 Critical は解消済み。持ち越しの Major 1件のみ要対応）

---

## 🔴 Critical Issues

なし

---

## 🟠 Major Issues

### [M-1] `TodoListCard` の「すべて見る →」が非インタラクティブ（前回レビューからの持ち越し）

**ファイル**: `src/client/components/dashboard/TodoListCard.tsx:27`
**問題**: `<span className="... cursor-pointer">すべて見る →</span>` はクリックしても何も起きない。`MilestoneCard` で指摘した同パターンが `TodoListCard` でも踏襲されてしまっている。

```tsx
// ❌ 現在のコード
<span className="font-[family-name:var(--font-geist-sans)] text-[#FF8400] text-[13px] cursor-pointer">
  すべて見る →
</span>
```

```tsx
// ✅ 修正案
import Link from "next/link";

<Link
  href="/tasks"
  className="font-[family-name:var(--font-geist-sans)] text-[#FF8400] text-[13px]"
>
  すべて見る →
</Link>
```

**理由**: キーボード操作・スクリーンリーダーともに機能しない。DeadlineCard:22 / MemberWorkloadCard:50 と合わせて合計3箇所を一括で `<Link>` 化すること。

---

## 🟡 Minor Issues

### [m-1] `MilestoneItem` / `PhaseProgress` 型が未使用のまま残存

**ファイル**: `src/types/dashboard.ts:28–38`
**問題**: マイルストーンカードとフェーズ進捗カードを削除したが、対応する型定義は残っている。使われていない型はコードベースを読む人の混乱を招く。

```ts
// ⚠️ 現在: 使われていない型定義
export interface MilestoneItem {
  id: string;
  name: string;
  date: string;
  status: MilestoneStatus;
}

export interface PhaseProgress {
  name: string;
  percent: number;
}
```

将来再利用する予定がない場合は削除を推奨。残す場合は `// 将来使用予定` コメントを追加。

---

### [m-2] `AnnouncementCard` のコンテンツが 1行 `truncate` のみ

**ファイル**: `src/client/components/dashboard/AnnouncementCard.tsx:31`
**問題**: `ann.content` は `truncate`（1行省略）で表示されているが、お知らせ内容がどんなに長くても完全に読めない。

```tsx
// ❌ 現在
<span className="... truncate">
  {ann.content}
</span>
```

```tsx
// ✅ 2行表示に変更する場合
<span className="... line-clamp-2">
  {ann.content}
</span>
```

`truncate` は1行でよいという設計意図であれば問題なし（設計確認推奨）。

---

## 🟢 Good Points

### [G-1] `STATUS_DOT: Record<TodoStatus, string>` の網羅的マッピング

**ファイル**: `src/client/components/dashboard/TodoListCard.tsx:9–13`
`Record<TodoStatus, string>` を使用しており、`TodoStatus` 型に値が追加されたとき TypeScript のコンパイルエラーで気づける。前回 `DeadlineCard` の `PRIORITY_STYLES` と同じ安全なパターンが踏襲されている。

### [G-2] `isDone` フラグによる明快な表示分岐

**ファイル**: `src/client/components/dashboard/TodoListCard.tsx:36, 45–47`
```tsx
const isDone = todo.status === "done";
// ...
className={`... ${isDone ? "line-through text-[#666666]" : "text-[#111111]"}`}
```
ステータスを変数に切り出してから参照しており、条件が一箇所に集まって読みやすい。

### [G-3] `TodoListCard` の `flex-1` によるレイアウト充填

**ファイル**: `src/client/components/dashboard/TodoListCard.tsx:21`
左カラムで `DeadlineCard` の下に配置するとき、`flex-1` が残りの縦スペースを自動的に充填する設計になっており、コンテンツ量に関わらずカード同士が隙間なく収まる。

### [G-4] `AnnouncementCard` の日付レイアウト

**ファイル**: `src/client/components/dashboard/AnnouncementCard.tsx:23–28`
`flex items-center justify-between` でタイトルと日付を左右に配置し、日付に `shrink-0` を付けてタイトルが長くても日付が潰れない設計が正確。

### [G-5] `TodoStatus` が `MilestoneStatus` と整合した型設計

**ファイル**: `src/types/dashboard.ts:2, 95`
```ts
export type MilestoneStatus = "done" | "in_progress" | "pending";
export type TodoStatus      = "pending" | "in_progress" | "done";
```
値のセットが共通で、順序だけ異なる。将来ユニオン型として統合できる一貫した設計。

---

## 🔵 Suggestions

### [S-1] `STATUS_DOT` のカラー定数を `PRIORITY_BADGE` と同ファイルに移動

**ファイル**: `src/client/components/dashboard/TodoListCard.tsx:9–13`

`TodoListCard` と `DeadlineCard` はどちらも `TaskPriority` バッジを扱う。ステータスカラー定数もすべて同じカード内に定義されているため、将来 `dashboard/constants.ts` にまとめると保守しやすくなる（任意対応、現状でも問題なし）。

---

## アクションアイテム

- [ ] [M-1] `TodoListCard` の「すべて見る →」を `<Link>` に変更（DeadlineCard:22 / MemberWorkloadCard:50 も同時対応） — **必須・今週中**
- [ ] [m-1] `MilestoneItem` / `PhaseProgress` 型の削除または残存理由コメント追加 — 推奨
- [ ] [m-2] `ann.content` を `line-clamp-2` 等で設計意図を明確化 — 確認後対応

---

## 前回レビューからの解消状況

| ID | 内容 | 状態 |
|----|------|------|
| C-1 | `total === 0` NaN | ✅ 解消済み |
| M-1 | cursor-pointer span (MilestoneCard 分) | ⚠️ MilestoneCard 削除で解消、TodoListCard に同パターン引継ぎ |
| M-2 | `badgeBg` をデータ型から除去 | 🔲 未対応（DeadlineItem に残存） |
| M-3 | DashboardSidebar を layout/ へ移動 | 🔲 未対応 |
| M-4 | Sidebar nav を `<Link>` に変更 | 🔲 未対応 |
| m-1 | MemberWorkloadCard 二重ボーダー | 🔲 未対応 |
| G-6 | PhaseProgressCard の3状態カラーロジック | ➖ カード削除により対象消滅 |

---

*Generated by Claude Code / code-review skill*
